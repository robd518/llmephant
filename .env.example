# These settings are referenced in src/llmephant/core/settings.py

# ---------- API Server Configuration ----------
API_HOST=
API_PORT=
API_HOT_RELOAD=

# ---------- MCP Server Configuration ----------
TOOLING_CONFIG_FILE=./tooling_config.yml

UPSTREAM_OPENAI_BASE=
UPSTREAM_OPENAI_API_KEY=
QDRANT_URL=
QDRANT_COLLECTION=
EMBEDDING_MODEL_NAME=

# ---------- Memory Service Environment Variables ----------
# Model used for distill/extract/verify passes.
# Example: Qwen3-4B-Instruct-2507-GGUF
MEMORY_MODEL_NAME=

# Minimum confidence (0..1) required to accept extracted user/workspace/profile items.
# Higher = fewer, cleaner memories. Recommended: 0.85–0.95
MEMORY_MIN_CONFIDENCE=0.9

# Near-duplicate threshold for semantic dedupe (0..1).
# Higher = more aggressive dedupe. Recommended: 0.88–0.95
MEMORY_SIMILARITY_THRESHOLD=0.92

# TTL (days) for stored memories.
MEMORY_TTL_DAYS=365

# Enable side-effectful memory extraction/storage.
# Set false in tests where you do not want writes.
ENABLE_MEMORY_EXTRACTION=true

# Optional per-pass token caps (ints). Leave blank to use backend defaults.
# These are strong levers for latency.
MEMORY_EXTRACT_MAX_TOKENS=400
MEMORY_DISTILL_MAX_TOKENS=450

# Second-pass verifier token cap (kept small on purpose).
MEMORY_VERIFY_MAX_TOKENS=300

# Optional backend-specific hint to reduce reasoning latency if supported.
# Examples: none, low, medium, high. Leave blank to avoid backend coupling.
MEMORY_REASONING_EFFORT=none

# Fallback user id if caller does not provide one. Leave blank to require user_id.
DEFAULT_USER_ID=

# ---------- OpenWebUI Environment Variables ----------
DEFAULT_USER_ROLE=
DEVICE_TYPE=
DOCKER=
ENABLE_ADMIN_CHAT_ACCESS=
ENABLE_FORWARD_USER_INFO_HEADERS=  # to associate memories to the user
ENABLE_LOGIN_FORM=
ENABLE_PASSWORD_AUTH=
ENABLE_SIGNUP=
WEBUI_URL=

ENABLE_OAUTH_SIGNUP=
ENABLE_OAUTH_ID_TOKEN_COOKIE=
ENABLE_OAUTH_PERSISTENT_CONFIG=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
OAUTH_CLIENT_INFO_ENCRYPTION_KEY=
OAUTH_MERGE_ACCOUNTS_BY_EMAIL=
OAUTH_PICTURE_CLAIM=
OAUTH_SESSION_TOKEN_ENCRYPTION_KEY=
OAUTH_UPDATE_PICTURE_ON_LOGIN=
OPENID_PROVIDER_URL=
WEBUI_SECRET_KEY=

# ---------- Custom Environment Variables (i.e. - MCP tokens, etc.) ----------
# i.e. MY_MCP_SERVER_AUTH_TOKEN=1234567890abcdef